# The public IP address of this server
host_listen: 99.76.54.132

# The path to the avatars directory of the frontend
avatar_dir: '/path/to/public/images/avatars'

# The path to the private RSA key
# Use generate-key-pair.rb to generate it.
private_key_path: /path/to/private.key

# The URL base which other servers will use to retrieve avatar image files
# from this server
avatar_url_base: 'http://domain-or-ip-without-trailing-slash'

# Hostnames or IPs for each server that this server should distribute posts,
# comments, etc. to
forest:
  # - 'host or ip'
  # - 'host or ip'
